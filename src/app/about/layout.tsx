export const metadata = {
  title: "Ferres - Sobre",
  description: "Sobre o Ferres",
};

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
